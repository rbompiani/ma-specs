import React from "react"

import { AmplifyGreetings } from '@aws-amplify/ui-react';

const Header = () => {
    return (
        <header>
            <AmplifyGreetings />
        </header>
    )
}

export default Header;